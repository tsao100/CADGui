{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build CADGui (make)",
            "type": "shell",
            "command": "make",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "使用 Makefile 編譯 Motif + GLX + tinyspline 專案"
        },
        {
            "label": "clean CADGui",
            "type": "shell",
            "command": "make",
            "args": ["clean"],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": []
        }
    ]
}
